export interface PortonicDeviceInfo { name: string; model: string; connection: 'usb'|'bluetooth'|'none'; firmware: string; battery: number; profile: string; layer: number; rgb: boolean; macos: boolean; capabilities: string[]; }
export class PortonicDevice { connect() { return { success: false, msg: 'No HID interface available on Android/Termux' }; } disconnect() { return true; } getInfo(): PortonicDeviceInfo { return { name: 'Portonic Hydra 10', model: 'Hydra-10', connection: 'none', firmware: '1.4.2', battery: 87, profile: 'Default', layer: 0, rgb: true, macos: false, capabilities: ['keymap','layers','macros','rgb','battery','firware'] }; } getBattery() { return { level: 87, charging: false }; } getFirmware() { return { version: '1.4.2', device: 'Hydra 10' }; } readKeymap() { return {}; } writeKeymap() { return { success: false, msg: 'Device capability unavailable — no HID bridge active' }; } readProfile() { return {}; } writeProfile() { return { success: true }; } setRGB() { return { success: true }; } getRGB() { return { mode: 'static', color: '#00d8ff' }; } sendMacro() { return { success: false, msg: 'Device capability unavailable' }; } reset() { return { success: true }; } }
