import{useStats}from"../services/storage";export default function Stats(){const{stats}=useStats();return(<div className="container"><h1 style={{fontSize:'2rem',fontWeight:800,marginBottom:'2rem',color:'var(--accent)'}}>📊 Statistics</h1><div className="stats-grid"style={{gridTemplateColumns:'repeat(auto-fit,minmax(200px,1fr))'}}><div className="stat-card"><div className="stat-value">{stats.totalTests}</div><div className="stat-label">Tests Completed</div></div><div className="stat-card"><div className="stat-value">{stats.bestWpm}</div><div className="stat-label">Best WPM</div></div><div className="stat-card"><div className="stat-value">{stats.avgWpm}</div><div className="stat-label">Average WPM</div></div><div className="stat-card"><div className="stat-value">{stats.bestAccuracy}%</div><div className="stat-label">Best Accuracy</div></div><div className="stat-card"><div className="stat-value">{stats.streak}</div><div className="stat-label">Daily Streak</div></div><div className="stat-card"><div className="stat-value">{stats.totalChars}</div><div className="stat-label">Characters</div></div></div><h2 style={{marginTop:'2rem',marginBottom:'1rem',fontWeight:700}}>Recent Results</h2><div style={{background:'var(--surface)',borderRadius:12,padding:'1rem',border:'1px solid var(--border)',maxHeight:300,overflow:'auto'}}>{stats.history.length===0?<p style={{color:'var(--text-muted)',fontSize:'.85rem'}}>No results yet. Start a test!</p>:stats.history.map((r,i)=>(<div key={i}style={{display:'flex',gap:'2rem',padding:'.75rem 0',borderBottom:i<stats.history.length-1?'1px solid var(--border)':undefined,fontFamily:"'JetBrains Mono',monospace",fontSize:'.85rem'}}><span style={{color:'var(--accent)',fontWeight:700}}>WPM: {r.wpm}</span><span style={{color:'var(--text-muted)'}}>Acc: {r.accuracy}%</span><span style={{color:'var(--text-muted)'}}>Err: {r.errors}</span><span style={{color:'var(--text-muted)'}}>{r.pack}</span></div>))}</div></div>)}
