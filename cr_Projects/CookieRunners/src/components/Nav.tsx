import{BookOpen,Home,Activity,User,Cog}from"lucide-react";interface Props{page:string;setPage:(p:string)=>void}const links=[{id:'home',label:'Home',Icon:Home},{id:'test',label:'Test',Icon:Activity},{id:'cookies',label:'Cookies',Icon:BookOpen},{id:'stats',label:'Stats',Icon:Activity},{id:'profile',label:'Profile',Icon:User},{id:'settings',label:'Settings',Icon:Cog}];export default function Nav({page,setPage}:Props){return(<nav className="nav"><div className="nav-brand"><span>🍪</span>Cookie Runners</div><ul className="nav-links">{links.map(({id,label,Icon})=>(<li key={id}><a href="#" className={page===id?'active':''}onClick={e=>{e.preventDefault();setPage(id)}}><Icon size={16}style={{marginRight:6,verticalAlign:'middle'}}/>{label}</a></li>))}</ul></nav>)}
