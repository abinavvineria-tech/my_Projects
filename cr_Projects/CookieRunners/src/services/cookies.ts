import{useState,useEffect}from"react";import{cookies}from"../data";import type{Cookie} from"./types";const STORAGE_KEY="cr_favs";export function useFavorites(){const[favs,setFavs]=useState<string[]>(()=>{try{const s=localStorage.getItem(STORAGE_KEY);return s?JSON.parse(s):[]}catch{return[]}});useEffect(()=>{localStorage.setItem(STORAGE_KEY,JSON.stringify(favs))},[favs]);const toggle=(id:string)=>setFavs(p=>p.includes(id)?p.filter(x=>x!==id):[...p,id]);const isFav=(id:string)=>favs.includes(id);return{favs,setFavs,toggle,isFav}}export function useCookieSearch(query:string,game?:string,rarity?:string){const q=query.toLowerCase();return cookies.filter(c=>{if(game&&c.game!==game)return false;if(rarity&&c.rarity!==rarity)return false;if(!q)return true;return c.name.toLowerCase().includes(q)||c.id.includes(q)||(c.tags||[]).some(t=>t.includes(q))||c.gameShort.toLowerCase().includes(q)});}
